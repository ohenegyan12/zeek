import ProfilePage from "@/templates/ProfilePage";

export const metadata = {
    title: "Profile - Zeek Admin",
    description: "Manage your account and settings",
};

const Page = () => {
    return <ProfilePage />;
};

export default Page;
