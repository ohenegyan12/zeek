import SettingsPage from "@/templates/SettingsPage";

export const metadata = {
    title: "Settings - Zeek Admin",
    description: "System and AI configuration",
};

const Page = () => {
    return <SettingsPage />;
};

export default Page;
