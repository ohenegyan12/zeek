import PricingPage from "@/templates/PricingPage";

export const metadata = {
    title: "Pricing - Zeek Admin",
    description: "Pricing and cost management dashboard",
};

const Page = () => {
    return <PricingPage />;
};

export default Page;
